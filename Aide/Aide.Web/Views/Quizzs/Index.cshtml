@model IEnumerable<Aide.Api.Models.Quizz>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdentityKeyMatiereNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdentityKeyMatiereNavigation.Titre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdentityKey">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdentityKey">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdentityKey">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
